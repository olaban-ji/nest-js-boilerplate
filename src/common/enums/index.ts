export enum IANATimeZoneDatabaseEnum {
  LAGOS = 'Africa/lagos',
}

export enum UserRoleEnum {
  ADMIN = 'admin',
  USER = 'user',
}

export enum StorageDriverEnum {
  CLOUDINARY = 'cloudinary',
  S3 = 's3',
}

export enum PaymentDriverEnum {
  STRIPE = 'stripe',
}
export enum HttpRequestMethodEnum {
  GET = 'GET',
  POST = 'POST',
  PUT = 'PUT',
  DELETE = 'DELETE',
  PATCH = 'PATCH',
  HEAD = 'HEAD',
  OPTIONS = 'OPTIONS',
  TRACE = 'TRACE',
  CONNECT = 'CONNECT',
}

export enum UploadFileCategoryEnum {
  AVATAR = 'avatars',
}

export enum StripeCurrencyEnum {
  USD = 'usd',
}

export enum EmailSubjectEnum {
  PASSWORD_RESET = '🔐 Reset Your Password Quickly & Securely',
}

export enum EmailTemplateEnum {
  PASSWORD_RESET = 'password-reset',
}
